#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir//bin/sh" ]; then
  "$basedir//bin/sh"  "$basedir/../../../Users/USER/AppData/Local/Yarn/Data/global/node_modules/.bin/firebase" "$@"
  ret=$?
else 
  /bin/sh  "$basedir/../../../Users/USER/AppData/Local/Yarn/Data/global/node_modules/.bin/firebase" "$@"
  ret=$?
fi
exit $ret
